intel_iommu=on iommu=pt kvm.ignore_msrs=1 kvm.report_ignored_msrs=0 amdgpu.dc=0

# I can start VMs with amdgpu.dc=1 but sometimes the host os fails to start the display manager again, also failing to shutdown correctly when pressing the power button.
